import {WizardTool} from './wizard.po';

describe('MapComponent', () => {
  let page: WizardTool;

  beforeEach(() => {
    page = new WizardTool();
  });

  // should...
  it('should be able to enter to map', () => {
  });
});
